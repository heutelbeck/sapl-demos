set "permit on emergency"
priority deny or abstain errors propagate
for action == "read" & resource == "time"

    policy "permit during emergency"
    permit
        "status".<mqtt.messages> == "emergency";

    policy "suspend off emergency"
    suspend
        "status".<mqtt.messages> != "emergency";
